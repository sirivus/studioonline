to upload to github huge files
git lfs install
git lfs track "*.psd"
git add .gitattributes
git add file.psd
git commit -m "Add design file"
git push origin main

npm start
npm run build
npm serve -s build
